import { useEffect } from "react";
import { useCart } from "../context/Cart.context";
import CartAndOrder from "../mycomponents/CartAndOrder";

const CartScreen = () => {
  return <CartAndOrder />;
};

export default CartScreen;
